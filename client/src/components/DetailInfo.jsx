import React from "react";
//import styles from "./DetailInfo.module.css";

const DetailInfo = ({ asteroid }) => {
  return (
    <>
      <p>{asteroid.name}</p>
    </>
  );
};

export default DetailInfo;
